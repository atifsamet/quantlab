# Local development
NEXT_PUBLIC_API_BASE=http://127.0.0.1:8000

# Production (Vercel): set the same key to https://quantlab-api-0mtu.onrender.com
# or rely on web/.env.production / the production fallback in lib/api.ts.
